@model AssignmenttMVC.Model.EventsModel


@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    @{

        <table class="table">
            <thead>
            <th>Missed Events</th>
            <th>Upcoming Events</th>
            </thead>
            <tbody>

                <tr>
                    @*@foreach (var evt in Model)
                    {
                        <td>
                             
                                    <li>@Html.ActionLink(@evt.Title, "ViewEvent", "BookReadingEvent", new { @evt.UserId }, null)</li>
                            
                        </td>
                    }*@
                </tr>
            </tbody>
        </table>

    }
</div>
